#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 1 || ! "$1" =~ ^[0-9a-fA-F]{40}$ ]]; then
  echo "usage: onecomputer-deploy <40-char-commit-sha>" >&2
  exit 64
fi

exec runuser -u azureuser -- \
  /home/azureuser/work/onecomputer/scripts/onecomputer/deploy-azure-vm.sh "$1"
