# OneComputer React Decision Dashboard

Small Vite/React static app for OneComputer.

Deploy:

```bash
pnpm onecomputer:deploy examples/react/decision-dashboard --runtime react-static --execute-aws
```
