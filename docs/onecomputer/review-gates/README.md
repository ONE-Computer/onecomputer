# Review Gates — phase index (superseded)

> **SUPERSEDED.** The 47 individual review-gate files that lived here were
> self-graded phase checkpoints authored by an LLM. The 2026-06-28 audit found
> them unreliable — several claim enforcement that is stamped
> `simulator_only_not_enforced` and never wired to a request gate. They have been
> removed. **Ground truth is [`AUDIT.md`](../../../AUDIT.md).**
>
> This index preserves the phase map so you can still see what was attempted,
> each row corrected to its **real** status.

## What the phases claimed vs what they actually delivered

| Phase                          | Claimed                                                                                   | Real status (verified)                                                                                                                                                                                                                       |
| ------------------------------ | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0                             | Runner review, roadmap reset                                                              | Process bookkeeping only — no code.                                                                                                                                                                                                          |
| P1-1 / P1-2 / P1-3             | Protective guardrails engine, simulator API, "enforcement fixtures final"                 | Merge logic real; **enforcement is `simulator_only_not_enforced`** (`protective-guardrails-service.ts:341,422`), never wired to a request gate. The P1-3 doc itself admitted "Enforcement is not yet wired into runtime/gateway call sites." |
| P1 (durable registry)          | Durable registry                                                                          | Prisma `PolicyRule` table exists; used for CRUD/blocklist, not live request matching in OSS.                                                                                                                                                 |
| P2-1 / P2-2 / P2-3             | Personal connector broker, registry API preview, closeout                                 | Broker throws on `accessMode !== "read_only"` but **never touches a network** — no upstream to enforce against.                                                                                                                              |
| P2 (app passport + VTI grants) | App passport, VTI grants                                                                  | Passport JSON is real and self-consistent; VTI grant verification is **local HMAC mock** (`server.mjs:11`).                                                                                                                                  |
| P3-1 / P3-2                    | VTI consent Trust Task seam, step-up retrieval gate                                       | Fail-closed consent/step-up logic is real (`vti-consent-service.ts:191-242`) but **never called by the retrieval path** — only by workflow fixtures.                                                                                         |
| P3 (policy evidence chain)     | Hash-chain evidence                                                                       | Append-only hash chain real **in the access gateway only**; unsigned; api-side trust services persist nothing.                                                                                                                               |
| P4-1 / P4-2 / P4-3             | M365 agent directory mailroom contract, API preview, closeout                             | `syncMode:"graph_preview_only"` is a string literal. No SharePoint connector. Outlook read-write not implemented.                                                                                                                            |
| P4 (gateway hardening)         | Gateway hardening                                                                         | The Rust gateway hardening is **genuinely real** (strongest part of the repo).                                                                                                                                                               |
| P5-1 / P5-2 / P5-3             | Legal MFA workflow, executive briefing, golden workflow closeout                          | Workflow contract/preview functions real; not wired to live agent execution.                                                                                                                                                                 |
| P5 (VTI/Affinidi seam)         | VTI/Affinidi integration seam                                                             | `affinidi-vti` is an **alias for generic HTTP fetch** (`server.mjs:272-278`); no Affinidi SDK/DID/JWT verification. See [`vti-affinidi-integration-seam-2026-06-21.md`](../vti-affinidi-integration-seam-2026-06-21.md).                     |
| P6-0 … P6-6                    | CISO console UX, dashboard baseline, privacy console, mobile polish, stakeholder demo     | Next.js UI surfaces exist and are substantial (~48k LOC TS). UX is real; the _governance_ it visualizes is largely preview/scaffold.                                                                                                         |
| P7-1 / P7-2 / P7-3             | Policy compiler preview, evidence pack + scorecard closeout, approval diff export         | "Move readiness from 93/100 to 95/100 and set workRemaining=false" — **a self-assigned score by decree**, not a measurement.                                                                                                                 |
| P8-1 / P8-2 / P8-3             | Production pilot scope, final evidence + negative path, final 95 scorecard + demo + retro | Scorecard hand-graded by the same author. Pilot closeout's key assertion is `pack.readinessScore === 95` — it checks the LLM's hardcoded number still equals itself.                                                                         |

## The honest score

The author's own `vti95-state.json` admits: **honest baseline 25/100**, controlled-pilot proof 50/100, "final" 95 a third higher number for the same product. Real working production-grade code is ~35-40% of what 95 implies. See [`AUDIT.md`](../../../AUDIT.md) for the full breakdown and the list of genuinely-real components to build on.
